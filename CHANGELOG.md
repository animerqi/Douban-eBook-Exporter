# 更新日志

本项目基于 [Violin9906/Douban-eBook-Exporter](https://github.com/Violin9906/Douban-eBook-Exporter)（v1.0，MIT License）增强而来。

## v1.17（当前）
- **双界面自动适配**：按 URL 判别界面类型——`reader/ebook/*`（完结书籍，分页式）走按键翻页流程；`reader/column/*`（未完结/专栏，滚动式）走慢速滚动流程；未知路径用 DOM 特征兜底
- 完结书籍界面：修复「章节已滚动到底部」误判（此前被误判为滚动式）
- 分页式等图片期间不计空转，图片多的书不会被误停

## v1.16
- 模式检测改用 `data-pid` 判别完结书籍/专栏（后因全页面误匹配，v1.17 改为 URL 判别）
- 分页式「等图片」只看当前页

## v1.15
- 修复：等图片加载的轮数误计入"空转"，导致图片多的书被误停
- 完成原因在弹窗中保留显示（不被轮询覆盖）

## v1.14
- 分页式卡住时自动恢复：重新激活阅读区 + 尝试点击"下一页"按钮 + keyup 事件
- 停止判定放宽至 36 秒
- 停止原因报告（弹窗显示「完成（原因）」）

## v1.13
- 合并两套界面流程：完结书籍（分页式，按键翻页）+ 未完结/专栏（滚动式，多页抓取）

## v1.12
- 弹窗状态同步：打开时自动查询真实抓取状态，防止误操作重置进度
- 进度定期存档（每 20 秒），误触跳转/刷新最多丢失一小段

## v1.11
- 全局图片去重：同一张图（URL）整本书只输出一次（修复豆瓣阅读单段落渲染多份相同图片的问题）

## v1.10
- 修复滚动容器误判：同时检测滚动容器与窗口，谁在滚动用谁（修复"滚到底不翻页"、进度一直 0%）

## v1.9
- 抓取循环全面加固：try/catch 保证循环不死、唯一定时器防并发、章节超时兜底、滚动双保险、错误上报弹窗

## v1.8
- 新增后台 Service Worker 负责下载（内容脚本无权调用 chrome.downloads）
- 修复"抓完不下载/点 stop 报错"
- 底部等待改为固定上限，杜绝卡死

## v1.7
- 内容脚本直接下载（后因 MV3 限制移至 v1.8 的 Service Worker）
- 回到前台自动重新加载图片

## v1.6
- 慢速滚动（每轮 90% 视口），让懒加载图片充分加载
- 段落"无图版 → 有图版"自动升级替换（谱例/插图晚到也能补上）
- SVG / canvas 兜底支持

## v1.5
- 章节标题从「下一篇」链接解析（修复所有章节标题相同的问题）
- 图片下载并内嵌为 base64 data URI，完全离线可读

## v1.4
- 自动点击「下一篇」逐章连续抓取整本书
- 进度持久化（chrome.storage），章节跳转整页刷新也能恢复

## v1.3
- 抓取 DOM 中全部 `.page` 容器（滚动式阅读器一次抓完整章）
- 内容哈希去重（不再依赖 data-pid）
- 拆掉豆瓣阅读的逐字防复制 span

## v1.2
- 内容去重修复（data-pid 缺失时不再无限重复抓取）
- 自动点击阅读区域激活阅读器翻页

## v1.1
- 扩展匹配范围从 `reader/ebook/*` 扩大到 `reader/*`（支持专栏/连载）
- 内容脚本自动注入兜底；状态提示

## v1.0（原版）
- Violin9906 的原始版本：仅支持完结书籍（分页式），按键翻页 + data-pid 去重
